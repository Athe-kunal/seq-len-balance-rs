# seq-len-balance-rs

## Installing evcxr (Rust Jupyter kernel)

To use Rust in Jupyter notebooks, install evcxr and register it as a kernel:

```bash
cargo install evcxr_jupyter
evcxr_jupyter --install
```

Then restart Jupyter (or refresh the page). Rust will appear as a kernel option when creating a new notebook.